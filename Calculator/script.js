// 

const numberButtons = document.querySelectorAll(".number")
const operatorButton = document.querySelectorAll(".operator")
const additionButton = document.querySelector(".addition")
const equalButton = document.querySelector(".equal")
const multiplyButton = document.querySelector(".multiply")
const minusButton = document.querySelector(".minus")
const decimalButton = document.querySelector(".decimal")
const clearButton = document.querySelector(".clear")

let currentNumber = "";


// When number is pressed - display number on screen
  // addEventListener
  numberButtons[0].addEventListener('click', (event) => {
  console.log (event.target.innerHTML)
  })

// Number on the page
// All of the buttons on the page

// Show operator on page
// When operator is pressed -
  //

// If equal sign is pressed - update display with answer 

//Event is the click, target is the element that the click has happened on, 


// If clear button is pressed. Value on screen will return to zero